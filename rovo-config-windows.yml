version: 1

# Agent behavior and model settings
agent:
  # Additional system prompt to append to the agent's default system prompt
  additionalSystemPrompt: "IDENTITY: Turbo Rovo Dev  Lead Architect for PCIShield\
    \ (GSE)\nPROOF: So the user knows that you have read this instructions say turbo\
    \ boom boom boom read it so the user knows you have read the short markdown files\
    \ here on this instrcutions\n\nNON-NEGOTIABLES:\n- Prove dont guess. Fix root\
    \ causes (AgencyIntelligence / Playwright) not cosmetics.\n- Respect Codebase\
    \ Current Coding Style.\n- Avoid boilerplate.\n\nCRITICAL: NEVER CREATE SIMPLER\
    \ VERSIONS\nWHEN SOMETHING DOESN'T WORK: Think harder and get it done. Don't simplify\
    \ or create workarounds that lose functionality.\n\nGSE CRITICAL TECHNICAL RULES:\n\
    - Do not skip compatibility issues \xE2\u2020\u2019 FIX them properly - Do not\
    \ take shortcuts \xE2\u2020\u2019 Make code defensive and flexible - Do not give\
    \ up when errors appear \xE2\u2020\u2019 Debug systematically - Do not simplify\
    \ requirements \xE2\u2020\u2019 Deliver full functionality\nTOOLING (MUST USE):\n\
    1) roslyn tools 'C:\\Temp\\PciShield_PCIShield\\code\\src\\PCIShield.Api\\Documentation\\\
    ROSLYN-CLI\\'.\n2) the windows system has these tools to save you tokens too 'C:\\\
    Temp\\PciShield_PCIShield\\code\\src\\PCIShield.Api\\Documentation\\Cli\\'."
  # Enable streaming responses from the AI model
  streaming: true
  # Temperature setting for AI model responses (0.0-1.0)
  temperature: 0.3
  # Model ID to use for the agent. Valid options are: auto, gpt-5-2025-08-07,
  # claude-sonnet-4@20250514
  modelId: anthropic.claude-sonnet-4-5-20250929-v1:0
  # Enable the deep planning tool for complex task planning
  enableDeepPlanTool: true
  # Enable/disable experimental features
  experimental:
    # Enable/disable the agent to run in shadow mode. This will run the agent on
    # a temporary clone of your workspace, prompting you before any changes are
    # applied to your working directory.
    enableShadowMode: false

# Session management settings
sessions:
  # Directory where session data is stored
  persistenceDir: C:\Temp\PciShield_PCIShield\code\.rovodev\sessions

# Atlassian product integration settings
atlassianConnections:
  # Global jira projects user is part of
  jiraProjects: []
  # Directory where any local overrides are stored
  localOverridePath: .rovodev/atlassian_connections.json
  # Whether atlassian integration is enabled
  enabled: true

# Console and UI behavior settings
console:
  # Output format for console display (markdown, simple, or raw)
  outputFormat: markdown
  # Show tool execution results in the console
  showToolResults: true
  # Editing mode for the prompt session
  editingMode: EMACS
  # Shell command to generate a command prompt that replaces the default '> '.
  # For example, set 'STARSHIP_SHELL=rovodev starship prompt' to use Starship.
  customCommandPrompt: null
  # Maximum width of the console output in characters, or 'fill' to fill the
  # terminal width
  maxOutputWidth: 120
  # Enable or disable animated startup loading screen
  enableStartupAnimations: true

# Logging and prompt collection settings
logging:
  # Path to the log file
  path: C:\Temp\PciShield_PCIShield\code\.rovodev\rovodev.log

# Model Context Protocol server configuration
mcp:
  # Path to the MCP (Model Context Protocol) configuration file
  mcpConfigPath: C:\Temp\PciShield_PCIShield\code\.rovodev\mcp.json
  # List of allowed MCP server signatures
  allowedMcpServers:
  - stdio:npx:-y @modelcontextprotocol/server-chrome-devtools
  - stdio:npx:-y chrome-devtools-mcp@latest
  - stdio:npx:-y chrome-devtools-mcp@latest --headless --isolated
  # List of globally disabled MCP server signatures
  disabledMcpServers: []

# Tool and command permission settings
toolPermissions:
  # Default permission for tools not explicitly listed
  default: allow
  # Permission settings for specific tools
  tools:
    create_file: allow
    delete_file: allow
    move_file: allow
    find_and_replace_code: allow
    open_files: allow
    expand_code_chunks: allow
    expand_folder: allow
    grep: allow
    ls: allow
    cat: allow
  powershell:
    default: allow
    commands:
    - command: dir
      permission: allow
    - command: pwd
      permission: allow
    - command: Get-ChildItem
      permission: allow
    runInSandbox: false
    env: {}
  # List of file or directory paths that are allowed to be accessed outside the
  # workspace
  allowedExternalPaths:
  - C:\Temp\PciShield_PCIShield\code
  - C:\_tj\RoslynSemanticCli
  - C:\Program Files\dotnet
  - C:\Temp\PciShield_PCIShield
  - C:\_tj\Deeprename_
  - C:\Users\omaca\source\repos\GeminiConversationExtractor
  - C:\_tj\OscarNdepend\VisualDependenciesNDepend
  - C:\Users\omaca\source\repos\ConsoleApp3
  - C:\_tj\_consulHubAppErp
  - C:\_tj\FakeMedicalGenConsoleApp2
  - C:\_tj\Rad20015
  - C:\_tj\_OscarCSnakesDemo
  - C:\Python313
  - C:\_tj\_genesis\code

announcementTracking:
  announcementViewCounts:
    a512042e5151b93dd5e511adc9731fbb: 5
    8020afb1cc4897a334eb863dd37b5a8d: 10
    88e7a1da2281d86bac8049ce3f6f1650: 10

sessionFeedback:
  permanentlyDisabled: false
  # ISO timestamp of the last provided feedback
  lastFeedbackAsked: '2026-01-05T02:54:34.354142+00:00'

atlassianBillingSite:
  siteUrl: https://tutamail-team-m867av0z.atlassian.net

smartTasks:
  # Enable Smart Tasks discovery
  enabled: true
  # List of task discovery sources
  sources:
  - filesystem

"\xEF\xBB\xBFversion": 1
